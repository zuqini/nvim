-- put machine specific lsp configs here
-- NOTE: run the following command to avoid tracking changes to this file
-- git update-index --assume-unchanged lua/plugins/lsp/my.lua

-- eg.
-- local lspconfig = require('lspconfig')

-- lspconfig.omnisharp.setup {}
-- OR
-- omnisharp_mono comes from mason so need to setup defaults manually
-- lspconfig.omnisharp_mono.setup {
--     on_attach = lspconfig.zuqini_custom.on_attach,
--     capabilities = lspconfig.zuqini_custom.capabilities,
--     flags = {
--         debounce_text_changes = 150,
--     }
-- }
